Put Sega Genesis / Megadrive ROMs here as *.BIN, *.GEN or *.MD
files (optionally .Z-compressed, e.g. Sonic.BIN.Z). Interleaved
*.SMD images are not supported -- convert them to .BIN first.
TempleGen lists this directory in its ROM picker; other files
(like this one) are ignored.
